---
phase: 02-component-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/scroll-reveal.tsx
  - src/components/landing/Footer.tsx
  - src/components/landing/Navbar.tsx
  - src/components/landing/HeroSection.tsx
  - src/components/landing/FinalCTASection.tsx
autonomous: true

must_haves:
  truths:
    - "ScrollReveal and StaggerContainer accept animationKey prop for re-animation"
    - "Footer displays translated link labels and copyright"
    - "Navbar displays translated nav items and CTA buttons"
    - "HeroSection displays translated badges, titles, subtitles, and CTAs"
    - "FinalCTASection displays translated title, subtitle, and CTA"
    - "All animations re-trigger when language changes via console"
  artifacts:
    - path: "src/components/ui/scroll-reveal.tsx"
      provides: "animationKey prop on ScrollReveal and StaggerContainer"
      contains: "animationKey"
    - path: "src/components/landing/Footer.tsx"
      provides: "Translated footer content"
      contains: "useTranslation"
    - path: "src/components/landing/Navbar.tsx"
      provides: "Translated navbar content"
      contains: "useTranslation"
    - path: "src/components/landing/HeroSection.tsx"
      provides: "Translated hero content with re-animation"
      contains: "key={i18n.language}"
    - path: "src/components/landing/FinalCTASection.tsx"
      provides: "Translated final CTA content"
      contains: "useTranslation"
  key_links:
    - from: "src/components/landing/HeroSection.tsx"
      to: "public/locales/*/translation.json"
      via: "t() function calls"
      pattern: "t\\(['\"]hero\\."
    - from: "src/components/ui/scroll-reveal.tsx"
      to: "motion.div"
      via: "key prop passthrough"
      pattern: "key={animationKey}"
---

<objective>
Extend animation utilities with language-aware re-animation and migrate simple components to use translation hooks.

Purpose: Establishes the animationKey infrastructure needed for all components, then applies translations to the 4 simplest components (no arrays, no complex state). This de-risks the core animation pattern before tackling complex components.

Output: ScrollReveal/StaggerContainer with animationKey prop. Footer, Navbar, HeroSection, and FinalCTASection fully translated with animations that re-trigger on language change.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-integration/02-RESEARCH.md
@public/locales/es/translation.json
@public/locales/en/translation.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ScrollReveal and StaggerContainer with animationKey prop</name>
  <files>src/components/ui/scroll-reveal.tsx</files>
  <action>
Add `animationKey?: string` prop to both ScrollRevealProps and StaggerContainerProps interfaces.

For ScrollReveal:
1. Add `animationKey` to destructured props
2. Pass `key={animationKey}` to the motion.div element

For StaggerContainer:
1. Add `animationKey` to destructured props
2. Pass `key={animationKey}` to the motion.div element

This is a non-breaking change - animationKey is optional, so existing usage continues to work.

Reference the research file Pattern 5 and Pattern 6 for exact implementation.
  </action>
  <verify>
Run `npm run build` - should compile without errors.
Verify TypeScript recognizes the new prop: grep for `animationKey` in scroll-reveal.tsx.
  </verify>
  <done>
ScrollReveal and StaggerContainer both accept optional animationKey prop that passes through to motion.div key attribute.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate Footer component to use translations</name>
  <files>src/components/landing/Footer.tsx</files>
  <action>
Import `useTranslation` from 'react-i18next'.

Replace the hardcoded footerLinks array with a function that uses `t()`:
```typescript
const { t } = useTranslation();

const footerLinks = [
  { href: "#programa", label: t('footer.links.program') },
  { href: "#faq", label: t('footer.links.faq') },
  { href: "#", label: t('footer.links.contact') },
  { href: "#", label: t('footer.links.terms') },
  { href: "#", label: t('footer.links.privacy') },
];
```

Move the footerLinks definition inside the component (after useTranslation hook call).

Replace hardcoded text:
- "Agentcamp" → `t('footer.logo.text')`
- "by Frutero" → `t('footer.logo.byline')`
- Copyright text → `t('footer.copyright')`

Keep socialLinks array as-is (aria-labels are accessibility, not user-facing translated content - "Twitter", "LinkedIn", "YouTube" are brand names).

No animation key needed for Footer (no motion.div with initial/animate).
  </action>
  <verify>
Run `npm run dev`, navigate to footer section.
Open browser console, run `i18n.changeLanguage('en')` - verify footer text changes to English.
Run `i18n.changeLanguage('es')` - verify Spanish text returns.
Check console for any "missing translation" warnings.
  </verify>
  <done>
Footer component displays translated content for logo text, byline, navigation links, and copyright. Language changes via console update the text.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate Navbar component to use translations</name>
  <files>src/components/landing/Navbar.tsx</files>
  <action>
Import `useTranslation` from 'react-i18next'.

Move navItems inside the component and use translations:
```typescript
const { t } = useTranslation();

const navItems = [
  { name: t('navbar.links.home'), url: "#", icon: Home },
  { name: t('navbar.links.program'), url: "#programa", icon: BookOpen },
  { name: t('navbar.links.results'), url: "#resultados", icon: BarChart3 },
  { name: t('navbar.links.faq'), url: "#faq", icon: HelpCircle },
];
```

Replace hardcoded strings:
- Desktop CTA button text → `t('navbar.cta')`
- Mobile CTA button text → `t('navbar.cta')`
- Floating mobile CTA text → `t('navbar.ctaMobile')`
- Logo text "Agentcamp" → `t('navbar.logo.text')`
- aria-label "Toggle menu" → `t('navbar.menuLabel')`

No animation key needed (Navbar has no motion.div with initial/animate).
  </action>
  <verify>
Run `npm run dev`, test navbar.
Open browser console, run `i18n.changeLanguage('en')` - verify nav items and CTA buttons change.
Test mobile menu (resize browser) - verify mobile nav items also translated.
Check console for warnings.
  </verify>
  <done>
Navbar component displays translated nav items, CTA buttons, and logo text. Desktop and mobile views both update on language change.
  </done>
</task>

<task type="auto">
  <name>Task 4: Migrate HeroSection component with animation re-triggering</name>
  <files>src/components/landing/HeroSection.tsx</files>
  <action>
Import `useTranslation` from 'react-i18next'.

Add translation hook destructuring both t and i18n (needed for key prop):
```typescript
const { t, i18n } = useTranslation();
```

Replace all hardcoded strings with t() calls:
- Badge "100% Virtual" → `t('hero.badge.virtual')`
- Badge "Programa de 4 semanas" → `t('hero.badge.duration')`
- Title lines → `t('hero.title.line1')`, `t('hero.title.line2')`, `t('hero.title.line3')`
- Subtitle problem → `t('hero.subtitle.problem')`
- Subtitle solution → `t('hero.subtitle.solution')`
- Primary CTA → `t('hero.cta.primary')`
- Secondary CTA → `t('hero.cta.secondary')`
- Urgency text → `t('hero.urgency')`

Add `key={i18n.language}` OR `key={\`element-${i18n.language}\`}` to each motion.div/motion.h1/motion.p that has initial/animate props. Use unique key prefixes to avoid React duplicate key warnings:
- Badges motion.div: `key={\`badges-${i18n.language}\`}`
- Title motion.h1: `key={\`title-${i18n.language}\`}`
- Subtitle problem motion.p: `key={\`subtitle1-${i18n.language}\`}`
- Subtitle solution motion.p: `key={\`subtitle2-${i18n.language}\`}`
- CTAs motion.div: `key={\`ctas-${i18n.language}\`}`
- Urgency motion.p: `key={\`urgency-${i18n.language}\`}`

DO NOT add key to background gradient motion.divs (they're decorative, no text, no need to re-animate).

Reference research Pattern 4 (Hero example) for exact placement.
  </action>
  <verify>
Run `npm run dev`, view hero section.
Switch language via console `i18n.changeLanguage('en')`:
1. Verify all text changes to English
2. Verify animations replay (fade in, slide up)
Switch back to Spanish - animations should replay again.
No console warnings.
  </verify>
  <done>
HeroSection displays translated badges, titles, subtitles, and CTAs. Animations re-trigger on language change. Background effects animate only once (on initial load).
  </done>
</task>

<task type="auto">
  <name>Task 5: Migrate FinalCTASection component with animation re-triggering</name>
  <files>src/components/landing/FinalCTASection.tsx</files>
  <action>
Import `useTranslation` from 'react-i18next'.

Add hook:
```typescript
const { t, i18n } = useTranslation();
```

Replace hardcoded strings:
- Title line 1 "Deja de experimentar." → `t('finalCta.title.line1')`
- Title line 2 "Empieza a operar." → `t('finalCta.title.line2')`
- Subtitle → `t('finalCta.subtitle')`
- CTA button text → `t('finalCta.cta')`
- Urgency text → `t('finalCta.urgency')`

Add `animationKey={i18n.language}` to each ScrollReveal wrapper:
```typescript
<ScrollReveal animationKey={i18n.language}>
```

This component uses ScrollReveal wrappers, not direct motion.div, so animationKey pattern from Task 1 applies here.

DO NOT add key to background motion.div (decorative, no text).
  </action>
  <verify>
Run `npm run dev`, scroll to final CTA section.
Switch language via console:
1. Verify title, subtitle, CTA button, and urgency text all change
2. Verify scroll reveal animations replay on language change
Test both directions (es→en, en→es).
  </verify>
  <done>
FinalCTASection displays translated title, subtitle, CTA, and urgency text. ScrollReveal animations re-trigger on language change.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Build check**: `npm run build` passes without errors
2. **Translation coverage**: No "missing translation" warnings in console for these 4 components
3. **Language switching**: All 4 components update text when running `i18n.changeLanguage('en')` and `i18n.changeLanguage('es')` in browser console
4. **Animation re-trigger**: Hero section and FinalCTA section animations replay on language switch
5. **No regressions**: Components still render correctly in Spanish (default language)
</verification>

<success_criteria>
- ScrollReveal and StaggerContainer accept animationKey prop
- Footer, Navbar, HeroSection, FinalCTASection use useTranslation hook
- No hardcoded Spanish strings remain in these 4 components
- HeroSection motion elements have key={i18n.language} pattern
- FinalCTASection ScrollReveal wrappers have animationKey={i18n.language}
- All animations re-trigger on language change
- Build passes, no console warnings
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-integration/02-01-SUMMARY.md`
</output>
